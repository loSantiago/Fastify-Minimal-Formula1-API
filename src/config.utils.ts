//Return a dir sourcer folder.
export const sourceDirectory = () => {
    return __dirname;
}

//Directories of project
export enum projectDirectories {
    Models = "/models/",
    Data = "/data/",
    Services = "/services/"
}